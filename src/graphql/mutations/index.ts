import * as createText from './create-text.graphql';
import * as updateText from './update-text.graphql';
import * as deleteText from './delete-text.graphql';

export const mutations = {
    createText,
    updateText,
    deleteText
};
